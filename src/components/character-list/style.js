import styled from 'styled-components'

const StyledMainContainer = styled.main`
    display: flex;
    flex-wrap: wrap;
    grid-area: content;
    max-width: 1200px;
    margin: 0 auto;
`

export {StyledMainContainer}